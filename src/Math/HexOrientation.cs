namespace FlatRedBall2.Math;

/// <summary>Determines whether a hexagon has a vertex or an edge at its top.</summary>
public enum HexOrientation
{
    /// <summary>A vertex points upward.</summary>
    PointyTop,
    /// <summary>An edge is horizontal at the top.</summary>
    FlatTop,
}
